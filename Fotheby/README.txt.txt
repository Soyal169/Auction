Requirements:-
1. Latest Version of python installed.(You can download Python from the official Python website.)
2. Install django in the project( pip install django )
3. migrate the database( python manage.py migrate )
4. now run the server( python manage.py runserver "http://127.0.0.1:8000/" ) (now this will run the server)
5. the username and password of the admin is 

username = soyal
password = 1234

Thank You :)